target: C7:09DA
call_count: 32
strong=0 weak=11 suspect=21 invalid=0
  JSR from C7:081F -> valid / weak / unresolved
    note: caller lives in unresolved bytes, so this is weak anchor evidence
  JSR from C7:083A -> valid / weak / unresolved
    note: caller lives in unresolved bytes, so this is weak anchor evidence
  JSR from C7:0850 -> valid / weak / unresolved
    note: caller lives in unresolved bytes, so this is weak anchor evidence
  JSR from C7:086B -> valid / weak / unresolved
    note: caller lives in unresolved bytes, so this is weak anchor evidence
  JSR from C7:087F -> valid / weak / unresolved
    note: caller lives in unresolved bytes, so this is weak anchor evidence
  JSR from C7:0895 -> valid / weak / unresolved
    note: caller lives in unresolved bytes, so this is weak anchor evidence
  JSR from C7:08A9 -> valid / weak / unresolved
    note: caller lives in unresolved bytes, so this is weak anchor evidence
  JSR from C7:08BF -> valid / weak / unresolved
    note: caller lives in unresolved bytes, so this is weak anchor evidence
  JSR from C7:0941 -> valid / weak / unresolved
    note: caller lives in unresolved bytes, so this is weak anchor evidence
  JSR from C7:0987 -> valid / weak / unresolved
    note: caller lives in unresolved bytes, so this is weak anchor evidence
  JSR from C7:09C9 -> valid / weak / unresolved
    note: caller lives in unresolved bytes, so this is weak anchor evidence
  JSR from C7:028B -> valid / suspect / resolved_data
    caller_range: C7:0200..C7:02FF (ct_note_backed_frozen_page_c7_0200_branch_fed_control_pocket_bad_start_or_dead_lane_reject)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:02BF -> valid / suspect / resolved_data
    caller_range: C7:0200..C7:02FF (ct_note_backed_frozen_page_c7_0200_branch_fed_control_pocket_bad_start_or_dead_lane_reject)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:02D6 -> valid / suspect / resolved_data
    caller_range: C7:0200..C7:02FF (ct_note_backed_frozen_page_c7_0200_branch_fed_control_pocket_bad_start_or_dead_lane_reject)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:02F0 -> valid / suspect / resolved_data
    caller_range: C7:0200..C7:02FF (ct_note_backed_frozen_page_c7_0200_branch_fed_control_pocket_bad_start_or_dead_lane_reject)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:03DB -> valid / suspect / resolved_data
    caller_range: C7:0300..C7:03FF (ct_note_backed_frozen_page_c7_0300_mixed_command_data_bad_start_or_dead_lane_reject)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:03FD -> valid / suspect / resolved_data
    caller_range: C7:0300..C7:03FF (ct_note_backed_frozen_page_c7_0300_mixed_command_data_bad_start_or_dead_lane_reject)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:0420 -> valid / suspect / resolved_data
    caller_range: C7:0400..C7:04FF (ct_note_backed_frozen_page_c7_0400_mixed_command_data_manual_owner_boundary_review)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:044B -> valid / suspect / resolved_data
    caller_range: C7:0400..C7:04FF (ct_note_backed_frozen_page_c7_0400_mixed_command_data_manual_owner_boundary_review)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:046E -> valid / suspect / resolved_data
    caller_range: C7:0400..C7:04FF (ct_note_backed_frozen_page_c7_0400_mixed_command_data_manual_owner_boundary_review)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:0499 -> valid / suspect / resolved_data
    caller_range: C7:0400..C7:04FF (ct_note_backed_frozen_page_c7_0400_mixed_command_data_manual_owner_boundary_review)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:04EE -> valid / suspect / resolved_data
    caller_range: C7:0400..C7:04FF (ct_note_backed_frozen_page_c7_0400_mixed_command_data_manual_owner_boundary_review)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:056C -> valid / suspect / resolved_data
    caller_range: C7:0500..C7:05FF (ct_note_backed_frozen_page_c7_0500_mixed_command_data_mixed_lane_continue)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:058C -> valid / suspect / resolved_data
    caller_range: C7:0500..C7:05FF (ct_note_backed_frozen_page_c7_0500_mixed_command_data_mixed_lane_continue)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:05CF -> valid / suspect / resolved_data
    caller_range: C7:0500..C7:05FF (ct_note_backed_frozen_page_c7_0500_mixed_command_data_mixed_lane_continue)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:05F7 -> valid / suspect / resolved_data
    caller_range: C7:0500..C7:05FF (ct_note_backed_frozen_page_c7_0500_mixed_command_data_mixed_lane_continue)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:0691 -> valid / suspect / resolved_data
    caller_range: C7:0600..C7:06FF (ct_note_backed_frozen_page_c7_0600_branch_fed_control_pocket_manual_owner_boundary_review)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:06ED -> valid / suspect / resolved_data
    caller_range: C7:0600..C7:06FF (ct_note_backed_frozen_page_c7_0600_branch_fed_control_pocket_manual_owner_boundary_review)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:070D -> valid / suspect / resolved_data
    caller_range: C7:0700..C7:07FF (ct_note_backed_frozen_page_c7_0700_mixed_command_data_bad_start_or_dead_lane_reject)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:07B2 -> valid / suspect / resolved_data
    caller_range: C7:0700..C7:07FF (ct_note_backed_frozen_page_c7_0700_mixed_command_data_bad_start_or_dead_lane_reject)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:07CA -> valid / suspect / resolved_data
    caller_range: C7:0700..C7:07FF (ct_note_backed_frozen_page_c7_0700_mixed_command_data_bad_start_or_dead_lane_reject)
    note: caller sits inside a closed data range, so this hit is suspect
  JSR from C7:07FF -> valid / suspect / resolved_data
    caller_range: C7:0700..C7:07FF (ct_note_backed_frozen_page_c7_0700_mixed_command_data_bad_start_or_dead_lane_reject)
    note: caller sits inside a closed data range, so this hit is suspect
