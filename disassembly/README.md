# `disassembly/` Status

This directory is a **legacy pass-note mirror**.

What it contains:
- older disassembly markdown grouped by pass ranges
- historical copies of pass-era notes up through pass `163`

What it is **not**:
- not the active manifest-backed pass-note path
- not the current seam-history lane

Use instead:
- `passes/disasm/` for active manifest-backed pass notes
- `docs/sessions/` for note-backed continuation work after pass `191`

This directory should be treated as historical reference only.
