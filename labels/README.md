# `labels/` Status

This directory is a **legacy label-note mirror**.

What it contains:
- older label markdown grouped by pass ranges
- historical copies of pass-era label notes up through pass `163`

What it is **not**:
- not the active label-note path for current manifest-backed work

Use instead:
- `passes/labels/` for active manifest-backed label notes
- current handoffs and continuation notes for live seam state

Treat this directory as historical reference only.
