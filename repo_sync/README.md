# `repo_sync/` Status

This directory is **historical reference only**.

What it contains:
- repo-first bootstrap and transfer snapshots from passes `163..171`
- pass-era sync packets that were used while the repo became the canonical workspace

What it is **not**:
- not the active seam workflow
- not the current source of truth for the live frontier
- not the place to start a new disassembly session

Use instead:
- `README.md`
- `docs/handoffs/chrono_trigger_master_handoff_session16.md`
- `docs/handoffs/chrono_trigger_resume_checklist_c6_9a00_a3ff.md`
- `docs/sessions/chrono_trigger_session15_continue_notes_48.md`

Keep this directory because it preserves transfer history, but treat it as historical context only.
